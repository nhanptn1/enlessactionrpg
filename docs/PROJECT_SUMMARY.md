# Endless Archer RPG — Project Summary, Skills, and Next Checklist

*Written 2026-07-10 as a resumption reference, mirroring the same doc written for the sibling TopDownAdventure project. Update this directly the next time meaningful work lands — there's no separate memory-system record of this project's status beyond this file.*

## 1. Summary

**What it is**: a Godot 4.7 vertical-portrait (720x1280) wave-survival auto-battler — a fixed-position archer at the bottom of the screen, enemies fall from the top, auto-fire combat, XP/leveling, boss every 10 waves. Architecture is deliberately **per-Resource data-driven** (`EnemyData`/`SkillData`/`ItemData`/`WaveData`, plus `MovementBehavior`/`AttackBehavior` Strategy-pattern Resources) — this is a real, intentional divergence from the sibling TopDownAdventure project's const-Dictionary pattern; the two are not meant to converge.

**Git state** *(updated 2026-07-12)*: 17 commits, `f9d67c4` ("Milestone 1: playable prototype") → `5a609df` ("Replace Bat/Wasp/Wraith art and add Armored Gargoyle enemy") — Phases A-C (HUD, items, roster expansion), the Arrow Rain/Skeleton Soldier bug fixes, the leftover-extraction-artifact cleanup, and the new monster art are now all committed locally, split into 7 logical commits. **Not pushed** — none of this has had an in-editor playtest pass yet (see §3 Phase D), so hold off pushing until that happens.

**Current playable scope**:
- **Player**: fixed at the bottom, never moves. A single active skill at a time, auto-fired on a timer, upgraded wholesale (not stacked) at fixed levels: Basic Shot (L1) → Multishot (L3) → Piercing Arrow (L5) → Arrow Rain (L8) → Trap Shot (L10). Stat upgrades (damage/cooldown/+1 arrow/crit/HP/shield/XP-gain) offered as a 3-choice popup on every level.
  - *(2026-07-11)* Real player art: 8 frames extracted from a supplied reference sheet (`image/main-char-rpg.png`, a forest-ranger elf archer matching `archer_rpg_art_bible.txt` exactly) replace the old generic placeholder — `archer_idle_01/02.png` (2-frame idle) and `archer_shoot_01..06.png` (draw → full-draw → release → reload cycle) in `Player.tscn`'s `SpriteFrames`. Only the sheet's side-profile frames (7-24) were used, not its front-facing frames (1-6), per the user's own call — this game's archer never needs to face the camera. `Sprite` node's `offset`/`scale` were retuned (`offset = Vector2(-4,-21)`, `scale = 0.9`) so the new, much-larger-canvas frames keep the same body-anchor height the old tightly-cropped placeholder had, so `AttackOrigin`'s existing `(0,-40)` local offset still lines up with roughly bow/hand height. Playtested and confirmed good by the user.
- **Enemies**: 11 species (Slime Scout, Goblin Runner, Bat Swarm, Stinger Wasp, Cursed Wraith, Skeleton Soldier, Wolf Beast, Shield Skeleton, Armored Gargoyle, Sapling boss-minion) sharing `EnemyBase.gd`, delegating movement/attack to shared `MovementBehavior`/`AttackBehavior` Resources (straight/zigzag/dive movement; contact/ranged attack). Two bosses now (`BossBase.gd` — independent phase/telegraph system, not `EnemyBase`-based; both share one script, differentiated by a new `attack_pattern_id` export, see Phase C below): Corrupted Forest Guardian (2 phases, sapling-summon) and Dark Ranger Commander (2 phases, projectile-fan/multi-zone-rain/teleport attacks). They alternate by boss-cycle number via `WaveManager.boss_pool`.
  - *(2026-07-12)* **Real art for Bat Swarm, Stinger Wasp, Cursed Wraith + new Armored Gargoyle enemy**, extracted from a supplied reference sheet (`image/monster.png`, 4 creatures × full idle/fly/attack/hit/death animation sets). Only idle+move frames were extracted (hit/death sprite frames are dead code — `enemy_base.gd` only ever calls `sprite.play("move")`, hit/death react via tween-only flash/fade, never a sprite-frame switch, so those states weren't worth extracting). Background was a solid dark blue-gray with a lighter reference-grid overlay (not a checkerboard this time) — a plain brightness-tolerance match left the grid-lines opaque and bridged every cell into one connected blob (largest-component selection then picked the grid mesh instead of the character). Fixed with a brightness-independent hue-ratio classifier (`B-R`/`G-R`/`B-G` channel-difference bands) that catches the background at all three of its brightness levels (dark divider/mid base/light grid line) while correctly excluding Armored Gargoyle's warm/neutral stone-gray body color, confirmed by direct pixel sampling before extracting. One frame (`gargoyle_fly_06`) had a bad component-selection split and was dropped rather than debugged further — Armored Gargoyle's move cycle is 5 frames, not 6. Bat Swarm and Cursed Wraith's old art (`bat_move_01-04.png`, `wraith_idle_01/move_01.png`) and Stinger Wasp's single-frame `wasp_move_01.png` were deleted (no longer referenced anywhere). Armored Gargoyle uses `dive_movement`/`contact_attack` (tier 2, tankier than Skeleton Soldier: 25 HP/100 speed/10 dmg), added to `procedural_enemy_pool`. **Not yet playtested in-editor.**
  - *(2026-07-11, Phase C)* **Shield Skeleton** (tank): reuses Skeleton Soldier's art (silver/blue tint via root-node `modulate`, matching the `_summon_saplings()` precedent for tinting a reused sprite), `straight_movement`/`contact_attack` behaviors (zero new behavior code), much higher HP/lower speed than Skeleton Soldier. Added to `procedural_enemy_pool` (post-wave-5 only).
  - *(2026-07-11, Phase C)* **Elite variant system**: `wave_manager.gd`'s new `_spawn_one()` rolls a 5% elite chance per spawn on non-boss waves (`ELITE_CHANCE`/`ELITE_HP_MULT`/`ELITE_SPEED_MULT`/`ELITE_DAMAGE_MULT`/`ELITE_XP_MULT` consts), and `enemy_spawner.gd` applies a gold tint + 1.2x scale bump on top of whatever `visual_scale` was already requested. No new EnemyData/scenes needed — reuses the existing hp/speed/damage-mult plumbing that already drove per-wave difficulty scaling.
  - *(2026-07-11)* **Fixed leftover checkerboard-background artifact on 7 pre-existing enemy sprites**: `skeleton_idle_01.png`, `skeleton_attack_01.png`, `wolf_move_01-04.png`, `wolf_attack_01.png` (all from commit `923ad44`/`0eabc3a`, well before this session) had real, unremoved checkerboard-background pixels baked into the PNG — confirmed via pixel inspection (not just a viewer/transparency-indicator illusion — verified other enemies like bat/bramble/golem/sproutling/wraith/wasp were actually clean). No source reference sheet survives to re-extract from, so these were cleaned in place: a neutral-gray color test (channel spread ≤5, since these creatures' real art is consistently tinted — purple cloak, warm bone/steel — while the checkerboard is exactly neutral) plus an 8-connected-largest-component pass to mop up stray fragments. `wolf_attack_01.png` needed two extra passes (its checkerboard happened to be warm-tinted, closer to its own legitimate dust-cloud VFX in color) using a tighter warm-hue-signature match to avoid eating the dust effect. Tradeoff: `skeleton_idle_01`/`skeleton_attack_01`'s armor has a handful of small stipple-holes now (a few genuine neutral-gray highlight pixels got caught by the same color test) — visible if zoomed in 3x+, not visible at the game's actual ~0.23x render scale. Since Shield Skeleton and Dark Ranger Commander (this session, above) both reuse this same skeleton art, they're automatically fixed too. Playtested and confirmed good by the user.
  - *(2026-07-11)* **Fixed leftover extraction-guide-line artifact on 12 arrow VFX sprites**: `arrow_basic_01-04.png`, `arrow_fire_01.png`, `arrow_lightning_02/03.png`, `arrow_poison_01-04.png`, `arrow_wind_03.png` (again pre-existing, from `9be5932`) had a thin vertical (sometimes crosshair-shaped) guide-line artifact baked in, visible as a stray blue line through the arrow. Fixed via spatial isolation (clear the line wherever it's disconnected from the real silhouette) plus, for the blue-toned lightning/wind variants where the line's color coincides with the design's own palette, a tight dark-navy-blue color-signature match confined to the exact 1-2 confirmed line columns.
  - *(2026-07-11)* **Fixed Arrow Rain (player L8 skill) firing sideways instead of down**: `scenes/effects/ProjectileRain.tscn`'s `Visual` node had `rotation = 1.5708` (90°) while every other projectile scene (`Projectile`/`ProjectileMultishot`/`ProjectilePiercingArrow`) uses `rotation = 3.14159` (180°) — a pre-existing authoring inconsistency (this skill predates this session). Combined with `Projectile.activate()`'s `rotation = direction.angle()` and Arrow Rain's straight-down `Vector2.DOWN` firing direction, the 90°-off Visual rotation made every rain arrow render rotated 90° from correct, i.e. sideways instead of pointing down — confirmed by the user's screenshot showing a horizontal row of sideways arrow sprites. Fixed by changing `ProjectileRain.tscn`'s rotation to match the other three scenes.
  - *(2026-07-11)* **Fixed Skeleton Soldier/Shield Skeleton reading as frozen despite actually moving**: both only had a single-frame "move" `SpriteFrames` animation (`skeleton_idle_01.png` alone — pre-existing for Skeleton Soldier, inherited into Shield Skeleton when it reused the same art), so the sprite never visibly changed pose while translating down the screen — user confirmed via testing that it "spawn[s] and move[s] down but no walking action." Every other enemy in the roster alternates between 2 real poses for its "move" animation; skeleton only has 2 total assets (`skeleton_idle_01`, `skeleton_attack_01`, no dedicated walk pose), so fixed by adding `skeleton_attack_01` as a second alternating "move" frame (speed lowered to 2.5 so the alternation reads as a sway, not a rapid flicker) for both `SkeletonSoldier.tscn` and `ShieldSkeleton.tscn`. **Known, accepted limitation, not fixed**: Stinger Wasp has only one art asset total (`wasp_move_01.png`, used for all of move/idle/attack) — same static-sprite symptom, but no second frame exists to alternate with short of new art, so left as-is.
  - *(2026-07-11, Phase C)* **`boss_base.gd` refactor**: the whole attack kit (`ATTACK_DATA`/`PHASE_1_ATTACKS`/`PHASE_2_ATTACKS`) used to be hardcoded consts, meaning only one boss could ever exist without duplicating ~230 lines of shared telegraph/phase/death framework. Moved into an `ATTACK_PATTERNS` dict keyed by a new `@export var attack_pattern_id`, so both bosses share one script. Dark Ranger Commander's kit needed 3 new special-cased methods beyond the existing generic shape-based telegraph system (mirroring how `summon_saplings` was already special-cased): `_rapid_volley()` (fires real pooled `CursedBolt` projectiles in a 3-shot fan — reuses `ProjectilePool`/`RangedAttack`'s exact infrastructure), `_arrow_rain()` (3 telegraphed impact zones scattered near the player, landing together), `_shadow_step()` (fade-out/reposition/fade-in teleport, no new VFX needed). `aimed_shot` reuses the existing generic `reach_line` shape as-is.
- **Systems**: `WaveManager` (5 hand-authored waves + procedural scaling beyond that, boss-cycle HP scaling), item/loot economy (24 hand-authored `ItemData` resources, rarity-weighted drops), save/load (best wave/level only, all-or-nothing validation), a full pause system (Escape key + on-screen HUD button, `GameManager` state machine with named pause sources), procedural chiptune audio via `AudioManager`, `Camera2D` shake, `WorldEnvironment` Glow (Mobile renderer), and a `ProjectilePool` (projectiles only — enemies are still `instantiate()`/`queue_free()` per spawn, not pooled).
- **HUD** *(2026-07-11, Phase A of the "stay small" plan)*: added a Boss HP bar (`SignalBus.boss_hp_changed`, emitted from `boss_base.gd` on spawn + every hit; shown/hidden via the existing `SignalBus.wave_started(_, is_boss)`) and a persistent skill display (`SkillLabel` + a procedurally-drawn `RadialCooldown` ring, `scripts/ui/radial_cooldown.gd` — a `Control` using `draw_arc()`, no texture asset needed; polled each frame from `player.attack_timer.time_left`/`.wait_time`, empty right after firing → full ring when ready). Confirmed good by the user, including the switch from an initial linear-bar version to the current radial one.
- **Items** *(2026-07-11, Phase B)*: audited all 24 `ItemData` resources for mechanical duplicates — found exactly one true duplicate (`apprentice_bow`/`hunter_bow`, both common/weapon/damage×1); fixed by changing `hunter_bow` to `crit_chance` (everything else in the 24 turned out to already be uniquely (category, rarity, upgrade_id) — the "items are just reskins" concern from the Phase 0 audit was narrower than it first looked). Also replaced the flat rarity-tinted-circle pickup visual with `scripts/systems/item_icon.gd` (`ItemIcon`, a `Node2D` with `_draw()`) — draws a distinct silhouette per `category` (bow/shield/gem/potion) still tinted by rarity color, so gear reads as different from consumables in the world per the art bible's item rules, without needing real icon art yet.

**Plan-vs-reality gap — resolved 2026-07-11, see §3 item 0.** Three newer planning docs (`plan/gameplay-character-plan.txt`, `plan/monster-waves-progression.txt`, `plan/skill-tree-plan.txt`) describe a substantially more ambitious vision than what's built. Asked the user directly which direction to take; answer was **stay small, polish first** — keep the current linear skill track and current wave scale, and spend the next phase on the gaps already promised by the *existing* scope (Boss HP bar, skill cooldown UI, item content depth, a 2nd boss, tank enemies) rather than adopting the branching skill tree / 20–100-monster-wave vision. One part of the gap was already resolved in code before this decision, just not in docs: `docs/GamePlan.txt` still says *"Player archer stays fixed at the bottom,"* but `player.gd` already has real A/D + mouse-drag left/right movement — that line is simply stale and should be corrected, no decision was needed there.

## 2. Skills & Lessons

Same base playbook as TopDownAdventure: the **`godot-2d-game-dev`** skill (`C:\Users\nhanp\.claude\skills\godot-2d-game-dev\SKILL.md`) covers the general techniques (verify loop, sprite extraction pipeline, Plan Mode conventions, correctness checklist, GH Pages deploy). What's specific to *this* project:

- **Strategy-pattern behaviors over full component-nodes.** `MovementBehavior`/`AttackBehavior` are stateless `Resource` subclasses shared across every `EnemyData` that references them (Godot dedupes `.tres` Resources by path at runtime — several species really do share one instance). This was a deliberate choice over splitting `EnemyBase` into full component Nodes (Health/Hitbox/HitFlash as separate nodes), made when comparing against an external architecture template — the reasoning: this project already commits to Resource-driven data everywhere, and full component-nodes would be inconsistent with that for no functional gain at this project's scale. **Any new behavior subclass must never store per-instance state on itself** — only read/write fields on the `enemy` node argument passed to every method.
- **The `:=` type-inference gotcha bit this project twice.** `var x := some_expr` fails to compile if the expression's static type resolves to `Variant` — most commonly when a variable typed as plain `Node` (e.g. from `get_first_node_in_group()`) has a member accessed that only exists on a subtype like `Node2D` (`.global_position`). Fix: use plain `=` instead of `:=` for anything chained off a generically-typed lookup, or explicitly type the intermediate variable (e.g. `Array[Node2D]` return types sidestep this entirely).
- **Don't assume a Godot API exists — verify it.** `GameManager` originally connected to `get_tree().current_scene_changed` to reset pause state on scene reload; that signal doesn't exist on `SceneTree` in Godot 4 at all, and it silently would have been a real runtime error the first time a Restart button was pressed. Fixed by adding an explicit `GameManager.reset_state()` method called directly from both restart handlers, right before `get_tree().reload_current_scene()` — more robust than a signal hook anyway, since it's deterministic rather than dependent on scene-tree timing.
- **`SceneTree.create_timer()` defaults `process_always` to `true`.** Any gameplay-critical timer (the boss's telegraph/cooldown/summon loop, a trap's arm/expire timers) needs `create_timer(duration, false)` explicitly, or it keeps counting down in real time even while `get_tree().paused = true` — the boss could keep landing hits on a "paused" screen otherwise. This is *not* what you want for music/UI-adjacent timers (e.g. the post-boss-victory music-restart delay), which should keep the default.
- **Centralize signal emission at the state-owner, not at every caller.** `SignalBus.enemy_hit` was originally emitted separately from `projectile.gd` and `trap_zone.gd` (each place that deals damage) — this silently missed the "instant_bomb" item effect, which damages enemies directly. Moved into `EnemyBase.take_damage()`/`BossBase.take_damage()` themselves so every current and future damage source is covered by construction.
- **No proactive headless test runs** — a standing, explicit user preference for this project specifically (headless Godot runs were found to hang/feel stuck). Verify changes via direct code re-reading instead, and say so plainly when a change hasn't been playtested in-editor.
- **Confirm before every `git push`**, every time, even mid-flow.

## 3. Next Working Checklist

0. **(2026-07-11) Plan-vs-reality gap — RESOLVED, "stay small" chosen.** Asked the user directly: keep the current linear skill track + current wave scale and spend the next phase on filling out what's already promised, vs. committing to the bigger `plan/` vision (branching Fire/Frost/Lightning `SkillTreeManager`, 20–100-monster waves, enemy pooling). User picked **stay small, polish first**. So: `plan/gameplay-character-plan.txt`'s free left/right movement claim is *also* already resolved as true (see below), but its skill-tree and wave-scale claims are explicitly **not** being pursued for now — treat `plan/skill-tree-plan.txt` and `plan/monster-waves-progression.txt` as a possible *later* initiative, not near-term backlog. `docs/GamePlan.txt` still says "archer stays fixed" — that line is stale (`player.gd` already has real A/D + mouse-drag movement) and should just be corrected to match reality, no decision needed there.
   Recommended near-term phased plan (small-scope path):
   - **Phase A — HUD gaps. DONE 2026-07-11**, confirmed good by the user (see Boss HP bar / radial cooldown entry above under Summary).
   - **Phase B — item content depth. DONE 2026-07-11** (see Items entry above under Summary) — the one real duplicate (`apprentice_bow`/`hunter_bow`) is fixed, and pickups now draw a category silhouette instead of a flat tinted circle. Not done: a real hand-authored icon-art pass (still procedural shapes, not actual pixel art) — only worth revisiting if the procedural shapes read poorly in-editor.
   - **Phase C — roster expansion. DONE 2026-07-11** (see Enemies entry above under Summary): Dark Ranger Commander (2nd boss), Shield Skeleton (tank), and the elite-variant system are all in, plus the `boss_base.gd` data-driven refactor that made a 2nd boss possible without duplicating the framework. **Not yet playtested in-editor** — this is real new gameplay content (new boss attack patterns, a new tank enemy, elite rolls), worth a real play pass before calling it done: does Rapid Volley's fan actually read as "3 arrows," does Arrow Rain's 3 zones feel fair vs. cheap, does Shadow Step's teleport look right, does Shield Skeleton actually feel tanky at its tuned HP/speed, and do elite tints show up clearly against normal enemies.
   - **Phase D — doc hygiene + playtest.** `docs/GamePlan.txt`'s stale fixed-position line is now fixed. Still open: give the pause/autoload/skill work (bdadb43, already committed), this session's new player art, and all of Phases A-C an actual in-editor playtest pass (none of it has had one yet per the standing "no headless runs" note below) — prioritize boss-pause freezing, Arrow Rain/Trap Shot feel, save/load roundtrip, the new archer's idle bob/recoil/arrow-spawn height, and everything called out in Phase C above. XP/level-up pacing has never had a real playtest pass either and is now further complicated by item stat-boosts and the newer enemy roster.
1. **Enemy pooling** — deferred along with the big-wave vision (see item 0). Only `ProjectilePool` exists today; revisit if/when the "go big" path is ever chosen.
2. Carried over: `docs/sound_music_plan.txt` is explicitly deferred — don't raise it proactively.
